import { BlockCharsDirective } from './block-chars.directive';

describe('BlockCharsDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockCharsDirective();
    expect(directive).toBeTruthy();
  });
});
